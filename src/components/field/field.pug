include ../icon/icon.pug
include ../input/input.pug
mixin yaField(iconName, placeholder, type, disabled=false)
        .field
          +yaIcon(iconName)
          +yaInput(placeholder, type, disabled)